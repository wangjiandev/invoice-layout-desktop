# 贡献指南

感谢参与票据排版助手的开发。

提交前请确保：

1. 不包含真实票据、个人身份信息、税号、行程或本地绝对路径。
2. 新功能包含必要的单元或组件测试。
3. `npm run lint`、`npm run typecheck`、`npm test` 和 `npm run package` 均通过。
4. Commit 信息清晰说明改动目的。

测试 PDF 必须完全合成，且不得复制真实票据上的姓名、号码、二维码或交易信息。
